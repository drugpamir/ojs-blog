const URL_PREFIX = "/chat";

const Chat = () => {
  return `
    <section class="chat-section">
        <div class="chat-title">Чат</div>
        <div class="chat-messages">Сообщения</div>
     </section>
     `;
};

export default Chat;
export { URL_PREFIX };
